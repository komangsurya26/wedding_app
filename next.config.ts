import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["monni.fragrancetheme.com", "tamubali.com", "masdianastudio.com", "drive.google.com"],
  },
};

export default nextConfig;
