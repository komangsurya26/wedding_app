import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["monni.fragrancetheme.com", "tamubali.com", "masdianastudio.com"],
  },
};

export default nextConfig;
